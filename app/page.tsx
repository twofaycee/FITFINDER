"use client";
import { useRef } from "react"; import { ArrowUpRight, Camera, Sparkles } from "lucide-react";
export default function Home(){const input=useRef<HTMLInputElement>(null);return <main className="page"><nav className="nav"><div className="logo">FITFINDER<span>AI</span></div><div className="navlinks"><a href="#how">How it works</a><a href="/closet">My Closet</a><a href="#pro">Pro</a></div><button className="btn" onClick={()=>input.current?.click()} style={{margin:0,padding:"10px 18px"}}>Start styling <ArrowUpRight size={15} style={{verticalAlign:"middle"}}/></button></nav><section className="hero"><div><div className="eyebrow">AI PERSONAL STYLIST</div><h1>ONE PIECE.<br/><em>WHOLE FIT.</em></h1><p>Upload something you already own. FITFINDER identifies the style, searches real products online, and builds complete outfits around it.</p><button className="btn" onClick={()=>input.current?.click()}>Upload an item <Camera size={17} style={{verticalAlign:"middle"}}/></button><input ref={input} type="file" accept="image/*"/></div><div className="panel"><div className="upload" onClick={()=>input.current?.click()}><div className="plus">+</div><h2>Drop your piece here</h2><p>Jacket. Shoes. Pants. Anything.</p><button className="btn"><Sparkles size={16} style={{verticalAlign:"middle"}}/> Choose photo</button></div></div></section><section className="section" id="how"><div className="eyebrow">THE SYSTEM</div><h2>From one item to a full rotation.</h2><p className="muted">No guessing. Your outfit starts with what you actually own.</p><div className="steps"><div className="step"><b>01</b><h3>Upload</h3><p>Give FITFINDER one piece from your closet and tell us where you're wearing it.</p></div><div className="step"><b>02</b><h3>Analyze</h3><p>AI reads color, silhouette, category, aesthetic and styling cues.</p></div><div className="step"><b>03</b><h3>Build the fit</h3><p>Get complete looks using real products, prices and retailers you can actually shop.</p></div></div></section></main>}